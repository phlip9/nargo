builds:
  exclude: []
  # TODO(phlip9): macOS + arm builds
  include:
  # doesn't seem to want to build this...
  # - "tests.x86_64-linux.garnix-all-checks"
  - "packages.x86_64-linux.garnix-check-shards.*"
  - "devShells.x86_64-linux.*"
